# Koyeb configuration file for Docker deployment
name: edumaster
service_type: docker
routes:
  - path: "/"
    dest: "/"
  - path: "/*"
    dest: "/"
ports:
  - port: 80
    protocol: http
  - port: 8000
    protocol: http
regions:
  - was