# Koyeb configuration file for static site deployment
name: edumaster
service_type: static
routes:
  - path: "/"
    dest: "/index.html"
  - path: "/*"
    dest: "/index.html"
ports:
  - port: 80
    protocol: http
regions:
  - was